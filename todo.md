[x] BIG BUG: images inline should not be associated with a page nor grid cells
[x] BIG BUG: QR Code placement is basically completely broken.
[x] BUG: grid placed images can have their top border cutoff (in progress of fixing) 
[x] BUG: images placed inline can have their left border cutoff if in the left column (?)
[] BUG: I think it always assumes full width header for non-rectangular article boxes


[x] BIG FEATURE: different layout.yamls for print and online
[x] FEATURE: add markdown-comment linebreak marker to allow for more granular nudging without affecting website rendering
[] FEATURE: add a formatting check where if there is not at least one line after a markdown header, move it to the next available column
[x] FEATURE: make debug (show bounding boxes)

[] FORMATTING: In the directory, on the last page, both columns should be symmetrical as possible

